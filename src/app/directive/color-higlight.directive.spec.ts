import { ColorHiglightDirective } from './color-higlight.directive';

describe('ColorHiglightDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorHiglightDirective();
    expect(directive).toBeTruthy();
  });
});
